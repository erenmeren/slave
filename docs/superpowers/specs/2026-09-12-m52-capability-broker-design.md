# M52 — Default-deny capability broker

Ninth milestone of the roadmap `2026-09-10-roadmap-m44-m56.md`, extending `SlavePermission` (M11), the M18 hook permission path (`scripts/pause-gate.sh`, `scripts/lib/permissions.sh`, `writePermissionsFile`) and `buildChildEnv` — named as extensions, per roadmap lines 19-23 and 57. Designed 2026-09-12 from an inventory of the permission model and its one writer, the two hook gates and their measured exit contract, the five nouns and the three meanings of "capability", worker identity, the Supervisor's tier table, the simulation boundary, the UI and the gates that pin all of it (session scratchpad `m52-explore.md`), with findings — never shapes — borrowed from `munder-difflin`'s integration broker and `agency-agents`' tool manifests. Every **Ruling** took the controller's direction under the user's standing approval; where the repository forces a different shape the ruling carries an inline `(verified: …)` note. "Slave" is this project's word for an AI worker.

**Goal.** A worker may do exactly what someone decided it may do, and nothing else. The gate stops asking "is this forbidden" and starts asking "is this allowed": an absent, stale or unreadable matrix denies, an ungoverned tool denies, a forged identity denies. A worker never holds a credential — it asks the orchestrator, by name, for one of a closed list of operations with typed parameters, and the orchestrator runs it in its own process with the secret the worker cannot see, recording what was asked and what happened. Identity stops being a path: every run carries a token issued at spawn, hashed on the row, and the verdict a gate reads is bound to it. The Supervisor may point at a wall; only a person moves it.

**Facts the design stands on.** `SlavePermission {id, slaveId, tool String, mode PermissionMode}` (`schema.prisma:539-548`) has one writer — `setSlavePermission` (`packages/control/src/permission.ts:96-118`), no delete verb, no CLI verb, no seed rows — and four readers that each do `writePermissionsFile(runDir, resolveDenyList(rows, provider))` (`tick.ts:665`, `planning.ts:535`, `review.ts:484`, `resume.ts:84`). `resolveDenyList` skips everything but `deny` (`permission.ts:53`) and `read_permission_verdict` treats an absent file as "no matrix in play: allow" (`scripts/lib/permissions.sh:91-93`) — default-ALLOW twice over, over six prose rows (`PERMISSION_TOOLS`, `permission.ts:18-26`) of which three collapse to `Bash` and one (`read secrets`) maps to nothing. `claudeFlags` hardcodes `--permission-mode bypassPermissions` (`flags.ts:35-40`), so the hook is the only gate; M3 measured that exit 2 fails CLOSED while 1/126/127 fail OPEN, and that a deny is carried in a JSON body at exit 0 (`scripts/pause-gate.sh:6-24`). One `node -e` per tool call reads payload and file in the same process (`permissions.sh:51-55`). `runDir = join(repoPath,'.slaveofai','runs',runId)` (`packages/control/src/paths.ts:33`) is inside the repository the worker edits, and the library records the self-policing hole in its own comment (`permissions.sh:57-59`). `buildChildEnv` spreads `...process.env` (`runtime/process.ts:157-165`); there is no credential model, no `SLAVEOFAI_RUN_ID`, and nothing binds a verdict to a slave. `RunKind {implementation, review, planning}` (`schema.prisma:593-597`) is closed; `Slave.runtimeRoles` is free text capped at `MAX_RUNTIME_ROLES = 20`. `tierOf` demotes everything under a halt (`policy.ts:47`) and `assign_capability` is `applied` (`:69-70`). The event catalogue holds 55 types (`packages/db/src/enums.ts:10-64`). `scripts/gate-fakes/` already holds three fakes; the gate roster ends at `gate:m51-breaker` with "26 gates" (`README.md:908`, `package.json:64`).

## 1. Rulings

- **R1 — Five nouns, five homes, and a permission is an OPERATION, never a capability.** CAPABILITY stays M47's taxonomy — what a worker is FOR (`Capability`, `Slave.capabilities[]`, `Task.requiredCapabilities`, `assign_capability`), untouched. SKILL stays the catalogue plus the pump's tally. PROFILE stays M46's `ProfileSpec`, whose `constraints[]` is prose the model reads and is explicitly NOT enforcement. PERMISSION becomes a closed vocabulary of OPERATIONS in `packages/domain/src/permission/kinds.ts`: `PERMISSION_KINDS = ['read_repo','write_repo','run_commands','network_fetch','read_secret','deploy_release']` with `PERMISSION_LABEL` (`Read the repository` / `Write source` / `Run commands` / `Fetch over the network` / `Read a secret` / `Deploy a release`), `PermissionKind`, and `TOOLS_BY_KIND: Record<PermissionKind, Record<ProviderKind, readonly string[]>>` — the inverted `CAPABILITY_TOOLS` (`permission.ts:39-46`), now naming the FULL governed toolbox per kind (`read_repo` → `Read`, `Glob`, `Grep`, `NotebookRead`, `TodoWrite`, `Task`, `Skill` / `read`; `write_repo` → `Write`, `Edit`, `NotebookEdit` / `edit`; `run_commands` → `Bash`, `BashOutput`, `KillShell` / `shell`; `network_fetch` → `WebFetch`, `WebSearch` / nothing; `read_secret` and `deploy_release` name NO vendor tool — they are broker grants only, which is exactly why `deploy prod` could never be expressed as a tool deny). `TOOL_VOCABULARY` is the per-provider union, pinned by `packages/domain/test/permission/kinds.test.ts` against the tool names the fixtures observe, so a vendor tool nobody mapped is a red test and not a silent hole. `SlavePermission.tool` becomes `kind PermissionKind` and gains `grantedBy String?` / `grantedAt DateTime @default(now())`; `@@unique([slaveId, kind])` replaces `@@unique([slaveId, tool])`. Migration `20260912180000_m52_broker`: add `kind` nullable → one deterministic `UPDATE` (`repo read`→`read_repo`, `source write`→`write_repo`, `run tests`/`create branch`→`run_commands` with **deny wins** on the collision and the redundant row deleted, `deploy prod`→`deploy_release`, `read secrets`→`read_secret`) → `DELETE` every unmapped row with `RAISE NOTICE 'm52: dropped % unmapped permission rows'` (dropped, not kept under an `other`: the column is a bare `String` with no check constraint, and under default-deny a dropped row denies, which is the safe direction, while an `other` kind would invent a grant nobody can name) → `SET NOT NULL` → drop `tool` and its index → add the new unique. *(verified: `read secrets` is not deleted for being "a lie" (`permission.ts:45`, `PermissionMatrix.tsx:84`) — R3 gives it teeth as the credential gate, which answers the inventory's Q7.)* **Answering Q15 explicitly: kept apart.** Permissions and capabilities never unify — a taxonomy key says what a worker is dispatched FOR, a permission kind says what any worker MAY DO, and the UI prints "Permissions" and "Capabilities" as two words for two things, never one for both. `enum PermissionMode {allow, deny}` survives unchanged with a sharper meaning: `allow` is a grant, `deny` is a person's explicit refusal, and ABSENCE is "never asked" — all three deny at the gate, and the three states are what `PermissionMatrix`'s ✓/✕/– glyphs (`PermissionMatrix.tsx:9-23,124-140`) have always drawn. *(verified: keeping `mode` rather than making the row a bare grant preserves the existing three-glyph vocabulary, keeps `flip()` (`:49-53`) honest for the first time, and spares `invalid_permission_mode` a retirement across its three refusal homes.)*

- **R2 — Default-deny, everywhere the gate looks, and the baseline is computed rather than stored.** `resolveDenyList` becomes `resolveGrants(rows, provider, runKind)` in `packages/control/src/permission.ts`, returning the ALLOWED vendor tools; `writePermissionsFile` writes `{"version":2,"runId":"…","tokenHash":"…","allow":[{"tool":"Read","kind":"read_repo"}]}`. Version 1 is not read: a pre-M52 file, an absent file, an unreadable file, a malformed file, a `allow` key that is not an array and a `version` that is not 2 all DENY — `read_permission_verdict`'s ALLOW-by-absence arms (`permissions.sh:64-93`) invert one for one, and the sentence "no matrix in play: allow" is deleted from the library's contract comment. A tool named by no kind DENIES (reason kind `ungoverned_tool`): denylists close instances, allowlists close the class, and `TOOL_VOCABULARY`'s tripwire is what keeps that from breaking a run when a vendor ships a seventh tool. `BASELINE_GRANTS: Record<RunKind, readonly PermissionKind[]>` in `packages/domain/src/permission/kinds.ts` — `implementation → read_repo, write_repo, run_commands`; `review → read_repo, run_commands`; `planning → read_repo` — is unioned into the resolution, so `resolveGrants` = `BASELINE_GRANTS[runKind] ∪ {rows with mode allow}` **minus** `{rows with mode deny}`. *(verified: the direction seeded baselines as rows in the migration's data statement and keyed them on the runtime role. `Slave.runtimeRoles` is free text capped at 20 (`packages/control/src/profile.ts:31`) and cannot key a constant, while `RunKind` is the closed enum the four resolution sites already differ on; and six seeded rows per worker is a new unique index colliding with web fixtures in five files, the exact hazard `apps/web/test/integration/{settings-snapshot,project-settings,org-routes,skills-snapshot,skill-call-totals}` carries. Computing the baseline gives "nothing that works today stops working" by construction, keeps the migration to one data statement, and makes `source` a projection (R7) rather than a column.)* The hook keeps ONE `node -e` per tool call: the same process now reads the allow list, compares `sha256(SLAVEOFAI_RUN_TOKEN)` to the file's `tokenHash` (R4) and answers. Failure paths still exit 2 — measured as the only non-zero status that fails closed — and a POLICY denial still returns a deny JSON at exit 0, because that is the form whose reason `parsePermissionDenyReason` can read (`gate.ts:62-88`); `PERMISSION_DENY_REASON_PREFIX = 'permission matrix denies'` stays byte-equal, its message becomes `permission matrix denies '<label>' (<tool>) for this slave`, and `PERMISSION_DENY_CAPABILITY` keeps its shell-variable name carrying a `PermissionKind` key. The `run.tool_denied.capability` payload FIELD NAME is frozen (renaming it breaks `permission-mapping.test.ts`'s byte-equal pin, `gate:m18`'s payload assertion and every stored row); only its vocabulary moves, from `'run tests'` to `'run_commands'`. Cursor's measured limitation stands and is now stated in the matrix copy rather than left implicit: on Cursor only the shell path is enforced (`permissions.sh:30-36`), so a ✓/✕ on a non-shell kind is advisory there. MOVED pins, not deleted: `packages/providers/test/permissions-lib.test.ts`'s eleven ALLOW-by-absence cases become DENY cases with the same names; `packages/control/test/permission-mapping.test.ts` becomes `TOOLS_BY_KIND` + `resolveGrants` per provider per run kind; `packages/control/test/integration/permission.test.ts` gains the delete verb.

- **R3 — A credential is an object the worker never holds, and a brokered operation is a named verb with typed parameters.** `model Credential { id, workspaceId, name, kind CredentialKind, envVar String, createdAt }`, `@@unique([workspaceId, name])`, `enum CredentialKind { deploy_token, git_token, api_key }` — the VALUE is never in the database and never in an event: the orchestrator reads `process.env[envVar]` at execution time and nothing else ever holds it. `buildChildEnv` stops spreading `process.env` and passes `CHILD_ENV_ALLOW` (`packages/providers/src/runtime/process.ts`): `PATH`, `HOME`, `USER`, `SHELL`, `LANG`, `LC_ALL`, `TERM`, `TMPDIR`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`, `NODE_EXTRA_CA_CERTS`, `SSL_CERT_FILE`, plus the four `GIT_*` identity vars and the run's own `SLAVEOFAI_*` channels — an explicit name list, never a prefix rule and never a denylist (`munder-difflin`'s five-name strip list was seven short of a live session's dump). `DATABASE_URL`, `SLAVEOFAI_SESSION_SECRET`, `SLAVEOFAI_PASSWORD` and every operator API key are gone from the child, and the gate asserts it. `BROKERED_OPERATIONS` in `packages/domain/src/broker/operations.ts` is a static manifest — `deploy_release: { grant: 'deploy_release', params: z.object({ environment: z.string().min(1).max(64).regex(/^[a-z0-9][a-z0-9_-]*$/), digest: z.string().regex(/^[a-f0-9]{7,64}$/) }) }` — an environment KEY and a digest, never a URL and never a path, so a parameter can never name a target the binding did not register. `model BrokerBinding { id, workspaceId, op String, command String[], credentialId String?, createdAt }`, `@@unique([workspaceId, op])`. *(verified: a table rather than `Workspace.brokerCommands Json` — `verifyCommands`/`setupCommands` are `String[]` on the workspace and a Json blob would be the first untyped command carrier in the schema, and a binding needs a real FK to `Credential`.)* The channel: `SLAVEOFAI_BROKER_CHANNEL` → `<runDir>/broker.ndjson`, the FOURTH file channel of the `SLAVEOFAI_PAUSE_FLAG` / `PERMISSIONS_FILE` / `TOOL_RESULTS` shape (`runtime/process.ts:138-171`), appended `O_APPEND` one bounded line per request and answered by the daemon at `<runDir>/broker-<requestId>.json`. The worker calls `orchestrator broker run deploy_release --environment staging --digest <sha>`; the CLI verb writes the request, polls for the reply to `BROKER_CLIENT_TIMEOUT_MS = 150_000`, prints the bounded output and exits with the op's status. *(verified: the direction had the CLI authenticate against the database. It cannot — R3's own allow-list removes `DATABASE_URL` from the child, so a CLI spawned in the worker's shell has no database. The file channel needs no secret, no port and no new server, and R4's token is what makes a forged line useless. No hook pattern rule is needed either: `run_commands` is in the implementer baseline, so the `Bash` line is already allowed, and command-string inspection stays out of scope as M18 ruled.)* `packages/control/src/broker.ts` authorises — token → `SlaveRun` → `Slave` → grants → binding — and refuses `identity_mismatch`, `permission_denied`, `not_brokered`, `simulation`, `invalid_params`, `run_not_live`; `apps/orchestrator/src/broker.ts` executes through the existing `runShellCommand` (`apps/orchestrator/src/shell.ts`: process-group kill, `COMMAND_OUTPUT_LIMIT = 16 KiB`) with `BROKER_TIMEOUT_MS = 120_000` and the credential added to that one child's environment alone. Two new events, the 56th and 57th: `broker.executed { op, environment, paramsHash, exitCode, durationMs }` and `broker.refused { op, reason }` — params are persisted verbatim here, unlike M51's `argsHash`, precisely because the registry's schemas make them bounded, non-secret and the whole audit value (`paramsHash` is `hashToolInput` over the full object, for correlation); the credential, the command template and the output are never in an event. A new event pays the nine sites M51 R2 lists once: `packages/domain/src/events/schema.ts`, `schema.prisma`'s `EventType` + the migration, `packages/db/src/enums.ts`, `supervisor/timeline.ts`'s `EVENT_LANE` (both `work`), `apps/web/src/components/activity/cards.tsx` (component + registry), `apps/web/src/lib/activityFilters.ts` (both under `tool_calls`), `apps/web/src/server/timeline.ts`, `packages/domain/test/events/schema.test.ts`, `apps/web/test/{activityFilters,activity-cards}` — twice.

- **R4 — Worker identity comes from the runtime context, and the verdict a gate reads moves out of the worker's reach.** `SlaveRun.runTokenHash String? @unique` — `sha256` hex of 32 random bytes issued at spawn at the four dispatch sites and ROTATED on every resume, so a token recovered from an old worktree is dead. The plaintext exists only in the child's environment: `SLAVEOFAI_RUN_ID` and `SLAVEOFAI_RUN_TOKEN`, added to `CHILD_ENV_ALLOW`'s explicit list and to `buildChildEnv`'s required inputs. Both the hook and the broker resolve run → slave from the token and nothing else; the broker compares with `timingSafeEqual` over the hashes, and a mismatch, an unknown token or a concluded run is `identity_mismatch`, which fails closed. The hook needs no database and no second spawn: `permissions.json` carries `tokenHash`, and the node process already reading the file compares it to `sha256(SLAVEOFAI_RUN_TOKEN)` — which closes the "point `SLAVEOFAI_PERMISSIONS_FILE` at a sibling run's file" hole, because a sibling's hash will not match this child's token. `runFilePaths` (`packages/control/src/paths.ts:33`) moves `runDir` out of the worktree to `<SLAVEOFAI_STATE_DIR ?? $XDG_STATE_HOME ?? ~/.local/state>/slaveofai/runs/<runId>`, created `0700`; the pause flag, the permissions file, the tool-results tap and the broker channel all move with it, both adapters keep re-deriving the path from `dirname(checkpoint.pauseFlagPath)` unchanged, and a pre-M52 `Checkpoint` resumes against its old absolute path with no migration. `.slaveofai/` stops being written into a repository at all, which retires the self-policing threat model comment (`permissions.sh:57-59`) instead of restating it: a worker can no longer delete the file that governs it, because it can no longer see it. Cursor's `.cursor/hooks.json` stays in the worktree — it is the vendor's required location — and remains a stated limitation.

- **R5 — The Supervisor may request, never grant.** 16th action `request_permission { slaveId, name, kind, kindLabel, why }`, tier `proposed` ALWAYS — listed in `tierOf`'s `proposed` arm beside `hire_from_catalog`, with a comment saying the halt short-circuit is redundant for it on purpose (`policy.ts:91-110`'s precedent). 16th situation `permission_blocked` (subject = slave id): a live or recently concluded run has `PERMISSION_TRIP_COUNT = 3` or more `run.tool_denied` events naming the same kind, raised by `observe` and subject to `filterFresh`'s 15-minute cooldown like every other. `why` is built by `permissionWhyFor(kind, count)`, a fixed sentence from a constant — never a model's words — which is how the action clears the tension the inventory names: `critical.ts:33-34` forbids the Supervisor ANSWERING about permissions and credentials in prose, and this action writes no prose. `carryOut` gains one arm calling `setSlavePermission(slaveId, kind, 'allow', principal)`, reachable only through `applyDecision(decisionId, 'human', principal)`. **No Supervisor action writes `SlavePermission`, `Credential` or `BrokerBinding` directly, and `assign_capability` stays `applied`** — because a capability is not a grant: giving a worker `security.application` changes what it is dispatched FOR and changes nothing about what the gate lets it do; that worker meets the same default-deny wall as every other. A person grants through the CLI — `permission grant --slave <id> --kind <k>`, `permission deny --slave <id> --kind <k>`, `permission revoke --slave <id> --kind <k>` (back to unset — the delete verb `setSlavePermission` never had), `permission list --slave <id>`, plus `credential add|list` and `broker bind|list` — and through the web route (R7). Every write appends `permission.changed { slaveId, name, kind, kindLabel, from: 'allow'|'deny'|null, to: 'allow'|'deny'|null, by }`, the 58th event. *(verified: a new event rather than `org.changed { entity:'permission' }` — `org.changed`'s `field` union is spelled in four places (M50 E11) and its payload has no room for the from/to/by triple the Advanced surface must print; and a permission change is not a roster change.)* The README's standing caveat stands and is restated in the spec: every account is a full operator, so "a person granted this" means an authenticated human, not a role (`README.md:811-814`); `gate:m20-auth` is unchanged.

- **R6 — Nothing crosses the simulation boundary.** `runBrokeredOperation` refuses `simulation` when the run's workspace is absent, archived, or carries a simulation origin — belt and braces, since a simulated role is not a `Slave`, holds no `SlavePermission` and can reach no `SlaveRun`. `Credential` and `BrokerBinding` are workspace-scoped; a `SimulationRun` binds to the CATALOG company and never to a workspace (`schema.prisma:1380-1381`), so no simulation row can name one. `packages/control/test/simulation-boundary.test.ts` gains a third assertion over the same two file sets it already walks (`packages/simulation/src/**`, `packages/control/src/simulation.ts` + `simulation/*.ts`): the source must not match `/broker|Credential|SLAVEOFAI_RUN_TOKEN/` — the broker module named by name, the way the existing case names `@slave-of-ai/providers`. Broker events are `ExecutionEvent` rows on a real workspace, never `SimulationJournalEntry`. `scripts/deny-all-gate.sh` is unchanged and stays the simulation's answer: that actor gets nothing, unconditionally.

- **R7 — Surfaces: two words for two things, in one vocabulary.** `DetailsGroupName` gains one member, `'permissions'`; `SlavePanel` gains a `permissions` `DetailsGroup` between `skills` and `messages` — one line per `PERMISSION_KINDS` member, ✓ (granted) / ✕ (refused) / – (never asked), printing `PERMISSION_LABEL` with the raw key on `data-kind` and in `title` (`docs/ia.md:9-22` rule 3). Under `advanced`, each line's policy sentence: `Baseline (implementation runs)`, `Granted by <name> on <date>`, `Refused by <name> on <date>` or `Never granted` — the `source` projection `grantsFor(rows, runKind)` in `packages/domain/src/permission/resolve.ts`, computed and never stored. Project Settings' `PermissionMatrix` moves to the kinds vocabulary — six columns of `PERMISSION_LABEL`, `grid(columns)` unchanged, the `read secrets` unenforced-copy replaced by the Cursor limitation sentence and by "a brokered operation, not a tool" for `read_secret` and `deploy_release`, and `flip()`'s unset→allow reading is now correct rather than accidentally so. The Organization row is unchanged (it answers "what is this worker for", and R1 keeps that separate). Routes: `PUT /api/w/[workspaceId]/slaves/[slaveId]/permissions/[kind]` `{ mode }` and `DELETE` on the same path, in the `w/[workspaceId]/slaves/[slaveId]/{profile,runtime-roles,lifecycle,release}` family behind `slaveControlResponse` (M50 E8's ruling); the legacy unscoped `apps/web/src/app/api/slaves/[slaveId]/permission/route.ts` moves there and `PermissionMatrix` retargets — one row, one URL space. Two activity cards, `BrokerExecutedCard` (tone `ok`, `deploy_release → staging · exit 0`) and `BrokerRefusedCard` (tone `blocked`, `deploy_release refused — permission denied`), plus `PermissionChangedCard`. `docs/ia.md` gains M52 notes on the Settings row (permissions in the kinds vocabulary), the SlavePanel paragraph (the new group) and the Activity row.

- **R8 — Gate.** `scripts/gate-m52-broker.mjs`, `gate:m52-broker` after `gate:m51-breaker` in `package.json` and in CI, README 26 → 27 gates (`README.md:908`, roster `:828-836`); real daemon, fake CLI, `SLAVEOFAI_REQUIRE_FAKE_CLI=1`, Playwright. `scripts/gate-fakes/fake-deploy.sh` is the fourth fake: it appends `environment|digest|token-present:<yes|no>` to a file the gate reads and exits 0, holding the credential the worker never sees. Stages: (1) a fresh worker's resolved grants are exactly `BASELINE_GRANTS.implementation` and `permissions.json` is `version 2` with an `allow` array; (2) a `WebFetch` call — outside the baseline — is denied by the hook, the run SURVIVES, exactly one `run.tool_denied { tool:'WebFetch', capability:'network_fetch' }`, zero `guardrail.tripped`, no pause, no `Checkpoint` (the m18 shape); (3) `permission grant --kind network_fetch` then a restart, and the same call passes, with `permission.changed` recorded; (4) `broker run deploy_release --environment staging --digest <sha>` succeeds, `fake-deploy.sh` recorded `token-present:yes`, the CHILD env dump taken at spawn contains neither `FAKE_DEPLOY_TOKEN` nor `DATABASE_URL` (both asserted), and `broker.executed { environment:'staging', exitCode:0 }` is on the timeline with the token nowhere in the event log; (5) the same call from a worker without the `deploy_release` grant → `broker.refused { reason:'permission_denied' }`; (6) a request line carrying another run's id, and one carrying a wrong token → `identity_mismatch`, twice, fail closed; (7) an op with no `BrokerBinding` → `not_brokered`; (8) a simulation-origin call → `simulation`; (9) the Supervisor raises `permission_blocked` after three denials and records `request_permission` as `proposed` — assert the `SlavePermission` row is unchanged until a human approves; (10) the browser: `SlavePanel`'s permissions group ✓/✕/– with labels, the Advanced policy line naming who granted and when, and the Settings matrix in the kinds vocabulary; (11) `runDir` is outside the repository and `<repo>/.slaveofai` does not exist. Moved pins, named: `gate:m18-skill-and-teeth` stage 1 seeds `{kind:'run_commands', mode:'deny'}`, pins `{tool:'Bash', capability:'run_commands'}` and asserts the Activity text `Bash denied — Run commands`; `permissions-lib.test.ts` and `permission-mapping.test.ts` as R2 names them; `runtime-process.test.ts` for the env allow-list; `adapter-start.test.ts` / `adapter-resume.test.ts` / `run-preparation.test.ts` for the token threading; `gate:m20-auth` and `gate:m38-supervisor` unchanged but for the action count. `gate:m14-fidelity` screenshots regenerated in a deliberate commit (the worker panel and the settings page).

- **Never a real model call; one vitest at a time; `web:build` last, never beside `next dev`; vocabulary `slave`; the migration is one deterministic data statement plus the column swap this milestone owns, applied to both databases with the Prisma 7 diff proof; `decide()` unchanged; the M47–M51 gates unchanged except where R8 names them; a refusal after a write inside a transaction throws; refusal kinds in three homes; labels never keys.**

## 2. Surfaces after M52
Domain: `permission/{kinds,resolve}.ts` (`PERMISSION_KINDS`, `PERMISSION_LABEL`, `TOOLS_BY_KIND`, `TOOL_VOCABULARY`, `BASELINE_GRANTS`, `grantsFor`), `broker/operations.ts` (`BROKERED_OPERATIONS`, param schemas, `BROKER_TIMEOUT_MS`), action `request_permission`, situation `permission_blocked`, events `broker.executed` / `broker.refused` / `permission.changed`, `EVENT_LANE` ×3. DB: `Credential`, `BrokerBinding`, `CredentialKind`, `PermissionKind`, `SlavePermission.{kind,grantedBy,grantedAt}`, `SlaveRun.runTokenHash`, three `EventType` members, one `SituationKind` member, migration `20260912180000_m52_broker`. Control: `resolveGrants`, `setSlavePermission(kind, mode, principal)` + `clearSlavePermission`, `broker.ts` (authorisation + refusals), `credential.ts`, six refusal kinds, the `carryOut` arm. Providers: `permissionsFilePathFor`'s v2 body, `buildChildEnv`'s `CHILD_ENV_ALLOW` + run token, `scripts/lib/permissions.sh` inverted, `scripts/pause-gate.sh`'s message. Orchestrator: `paths.ts`'s state dir, token issue at the four dispatch sites, `broker.ts` execution + channel tail, CLI `permission` / `credential` / `broker`. Web: `SlavePanel` permissions group, `PermissionMatrix` kinds, two routes, three activity cards, `docs/ia.md`. Gate m52; `scripts/gate-fakes/fake-deploy.sh`.

## 3. Out of scope
`git_push` as a brokered op (there is no `git push` and no remote anywhere in `apps/orchestrator` or `packages/control`; it would need a faked remote as well as a faked credential, and `deploy_release` proves the same mechanism with one fake) — named as the second op the registry is shaped for. OAuth, vaults or any credential store beyond "a name and an env var the operator already exports". Per-path or per-command file permissions, and command-string inspection (M18's ruling, unchanged). Cursor hook parity beyond the existing shell gate. Rate limits, quotas or approval queues on brokered ops. A role model for operators (`README.md:811-814` stands). Revoking a credential mid-run, and brokered ops from a planning or review run beyond what their baseline allows. Editing `BrokerBinding` from the UI (CLI only, like `set-lifecycle`). M53 ranking, which is the first consumer of `broker.executed`.

## 4. Errata — where execution corrects this spec


Found while writing the plan (2026-09-12, before Task 1); each is baked into `docs/superpowers/plans/2026-09-12-m52-capability-broker.md`. Reasoning with file citations is in the plan's companion notes (session scratchpad `m52-plan-notes.md`); summarised here.

- **E1 (amends R2) — `SLAVEOFAI_PERMISSIONS_FILE` UNSET must still ALLOW, or every spawn fails.** R2 inverts "an absent file allows" into "an absent file denies". Taken literally that breaks the pre-flight every run already depends on: `preflightGate` (`packages/providers/src/runtime/gate-preflight.ts:98-141`) spawns the real `pause-gate.sh` twice per spawn and REQUIRES the second, disarmed, invocation to ALLOW, and `runGateScript` passes `{ ...process.env, SLAVEOFAI_PAUSE_FLAG }` (`:33`) — the orchestrator's own environment, which names no permissions file. A rule that denied on "unset" would fail that assertion on every start and every resume, on both providers, and no run would ever spawn again. The split is therefore between the VARIABLE and the FILE: **unset** = "this process is not a governed run" (a pre-flight, an operator running the script by hand) = ALLOW, unchanged; **set, but the file is absent, unreadable, malformed, not version 2, or carries no `allow` array** = DENY, fail closed. Belt and braces: `runGateScript` additionally DELETES `SLAVEOFAI_PERMISSIONS_FILE` from the child's environment, so the pre-flight measures the pause gate and never the matrix, whatever the operator's shell holds. A worker cannot reach this arm — the hook is spawned by the vendor CLI with the CLI's own environment, and a `Bash` call's subprocess environment is not the next `PreToolUse` hook's.
- **E2 (amends R2) — the deny reason keeps the KIND in its quoted slot; the LABEL goes on the card.** `PERMISSION_DENY_REASON_PATTERN` (`packages/providers/src/gate.ts:62-64`) parses the quoted group into `GateOutcome.tool_denied.capability` (`:82-88`), and `apps/orchestrator/src/pump.ts` writes that string verbatim into `run.tool_denied.payload.capability`. R2 freezes that field's vocabulary at `run_commands` and R8 stage 2 asserts `capability: 'network_fetch'` — both of which a `'<label>'` in the reason would break, because the payload would then read `Run commands`. The message is `permission matrix denies 'run_commands' (Bash) for this slave`; the ia.md rule-3 half R2 wanted lands on the CARD, where `RunToolDeniedCard` prints `TOOL_DENIED_LABEL[payload.capability] ?? payload.capability`. Nothing prints the reason string itself: for a parsed matrix deny it is consumed and discarded.
- **E3 (amends R2) — default-deny would refuse every tool call on CURSOR, so the file carries the vocabulary and an `enforce` word.** `scripts/lib/permissions.sh:30-36` records the measurement: Cursor's `preToolUse` sends Claude-shaped casing (`"tool_name":"Read"/"Shell"/"Write"`) which never matches `permissions.json`'s lowercase Cursor vocabulary. Under a denylist that mismatch is inert; under an allowlist it denies EVERY Cursor tool call, so the milestone would brick the provider it claims only to limit. `ProviderCapabilities.gate` cannot be the discriminator — Cursor's is `'all-tools'` (`packages/providers/src/capabilities.ts:90`), proven for the PAUSE path, which does not read a tool name at all. v2 therefore carries `"enforce": "all-tools" | "known-tools"` and `"vocabulary": { "<tool>": "<kind>" }`, the provider's whole `TOOLS_BY_KIND` inversion. The rule: on `allow` → ALLOW; in `vocabulary` but not on `allow` → DENY naming that kind; in neither → DENY as `ungoverned_tool` under `all-tools`, ALLOW under `known-tools`. `enforce` is `known-tools` for `cursor` and `all-tools` for `claude_code`, and the matrix copy says so in words.
- **E4 (amends R4 and R8 stage 11) — `.slaveofai/` does not stop being written into a repository; only `runs/` moves.** `apps/orchestrator/src/worktree.ts:24` puts every worktree under `<repo>/.slaveofai/worktrees`, `verify.ts:460` and `merge.ts:208` put every artifact under `<repo>/.slaveofai/artifacts`, and `apps/web/src/app/api/w/[workspaceId]/tasks/[taskId]/artifacts/[artifactId]/route.ts:20` resolves a download under that same root. Stage 11 therefore asserts that `<repo>/.slaveofai/runs` does NOT exist and that `runFilePaths(...).runDir` is outside `repoPath` — not that `.slaveofai` is absent, which would be false on any repository that has ever run a task.
- **E5 (amends R3 and §2) — one refusal kind carrying SEVEN reasons, not six refusal kinds.** The reasons are already a closed vocabulary in the `broker.refused` payload; spelling the same list again as `ControlRefusal` members would be two lists, and two of the names (`permission_denied`, `simulation`) are ambiguous beside the union's existing `invalid_provider` / `unsupported_simulation` / `simulation_not_found`. `BROKER_REFUSAL_REASONS` lives in `packages/domain/src/broker/operations.ts` with a label table; `ControlRefusal` gains exactly two kinds — `broker_refused { op, reason }` (409) and `credential_not_found { name }` (404 by `refusalStatus`'s `_not_found` suffix rule, `apps/web/src/server/refusalStatus.ts:11`). The seventh reason is `credential_unset`: a binding naming a credential whose `envVar` is absent from the daemon's own environment, which is the first thing an operator meets on a fresh install and which `not_brokered` would describe falsely.
- **E6 (amends R3) — there is no `orchestrator` binary; the worker runs `node "$SLAVEOFAI_BROKER_CLI" broker run …`.** `package.json:23` is `"orchestrator": "tsc --build && node --env-file=.env apps/orchestrator/dist/cli.js"` — an npm script, not something on a worker's `PATH` — and every gate invokes the CLI by absolute path (`scripts/gate-m51-breaker.mjs:84`). The path reaches the child as `SLAVEOFAI_BROKER_CLI`, carried the way `hookPath` and `tapPath` are: an ADAPTER OPTION filled once in `buildAdapterRegistry` (`apps/orchestrator/src/cli.ts:721-746`) from `fileURLToPath(import.meta.url)` — which IS `dist/cli.js` — and overridable by the same-named environment variable, exactly as `hookPath()` (`:580-585`) and `tapPath()` (`:597-623`) already are. Never a per-run input, never threaded through `StartRunInput`.
- **E7 (amends R4) — the run token cannot ride on `Checkpoint`, and it must not be written to a file.** `packages/providers`' `Checkpoint` may not gain a field with no matching Prisma column (its own docstring, `packages/providers/src/claude/checkpoint.ts:24-`), and a token file inside `runDir` would be readable by every sibling run under the same uid — reopening the sibling-forgery hole R4 exists to close. So: `StartRunInput.runToken?: string` and a FOURTH parameter on `resume(runId, checkpoint, queuedInstruction, runToken?)`. Both OPTIONAL, and that cannot widen anything: an absent token means the key is absent from the child's environment, and the child then meets a `tokenHash` it cannot match — every tool call denied. The ~20 existing `adapter.resume(...)` call sites in `adapter-resume.test.ts` and `cursor-adapter.test.ts` compile unchanged.
- **E8 (amends R2) — a payload that names no tool DENIES at exit 0, never at exit 2.** Three arms allow today (`scripts/lib/permissions.sh:68-81`): no `tool_name`, a payload that parses to a non-object (`null`, `[1]`, `7` — a real security-review finding), and an empty list. Under `all-tools` the first two now deny, reported as tool `unknown` with kind `ungoverned_tool`, at exit 0 in a deny BODY. Not exit 2: exit 2 fails closed by STOPPING THE RUN (`scripts/pause-gate.sh:13-20`), and a `PreToolUse` payload this gate cannot read is a refused CALL, not a broken gate. Under `known-tools` they still allow, which is the measured Cursor behaviour E3 preserves.
- **E9 (amends R5) — `permission_blocked` needs a world the loader does not build.** `SupervisorWorld` holds no events and no per-slave denial counts (`packages/domain/src/supervisor/world.ts:300-340` lists eleven collections plus M51's `runs`). It gains `denials: readonly SupervisorDenial[]` — `{ slaveId, kind, count, latestRunId }` — from ONE `$queryRaw` grouping `run.tool_denied` by `slaveId` and `payload->>'capability'` over `PERMISSION_DENIAL_WINDOW_MS`, modelled line for line on `loadLatestGuardrails` / `loadLatestBreakerTrips` (`packages/control/src/supervisorWorld.ts:271-325`) and SKIPPED entirely when the workspace has no run at all in the window — the loader pays for nothing a quiet project does not need.
- **E10 (amends R8) — `gate:m18`'s fixture is a genuine capture, so its vocabulary moves by a documented REDACTION row, not by a rewrite.** `packages/providers/test/fixtures/permission-matrix-deny.ndjson` is a real `claude` recording (`fixtures/README.md:6-8` and its own provenance section at `:191-`), and the deny reason inside it is OUR gate's own sentence echoed back by the CLI. `README.md:311-390` already carries a substitution table with a count per substitution and a runnable `sed` recipe; M52 adds one row — `'run tests'` → `'run_commands'`, **4 occurrences** — so `scripts/capture-matrix-deny.mjs` stays complete provenance for the next capture. The alternative, a second synthetic fixture, would put two recordings of one event in a namespace whose README says there is no hand-authored JSON in it.
- **E11 (amends R1) — `invalid_tool` keeps its name; only its sentence moves.** Renaming a refusal kind costs three homes — the union and `refusalText` (`packages/control/src/refusal.ts:214,502`), the CLI, and `apps/web/test/refusal-status.test.ts`'s `Record<ControlRefusal['kind'], true>` at `:66` — to rename a word no surface prints. Its payload field stays `tool` and now carries the offered kind string; its text becomes `a permission must name one of the six operations`.
- **E12 (amends R2 and §2) — `resolveGrants` is pure and belongs in the DOMAIN, beside `grantsFor`.** R2 puts it in `packages/control/src/permission.ts` while R7 puts `grantsFor` in `packages/domain/src/permission/resolve.ts` — two homes for one computation, which is the exact shape this repository refuses. It is also the M51/E1 situation again: `apps/web/src/components/PermissionMatrix.tsx:40-43` records that a `'use client'` component may not import `@slave-of-ai/control`, because that barrel re-exports `@slave-of-ai/providers`, which imports `node:child_process` at module scope. Both functions live in the domain; `packages/control/src/permission.ts` keeps only the WRITERS — `writePermissionsFile`, `setSlavePermission`, `clearSlavePermission`.
- **E13 (amends R1/R3/R5) — the counts are one out.** `packages/domain/src/events/schema.ts` carries 55 `z.literal` arms today (M51's `run.tool_result` and `run.breaker` were the 54th and 55th), so M52's three are the **56th, 57th and 58th** and `packages/domain/test/supervisor/timeline.test.ts:19` moves 55 → 58. `SITUATION_KINDS` has sixteen members since M51's `run_looping`, so `permission_blocked` is the **seventeenth** and `situations.ts:141-142`'s "a SEVENTEENTH kind fails the build" becomes eighteenth. The action union has sixteen members since `steer_run` (`packages/domain/src/supervisor/actions.ts:11-84`), so `request_permission` is the **seventeenth**.
- **E14 (amends R7) — the matrix already carries two captions that contradict each other, and a third contradiction in its server module.** `PermissionMatrix.tsx:84` says denials ARE enforced at dispatch snapshot; `:178-180` says `not yet enforced at runtime`; `apps/web/src/server/settings.ts:116-118` says an unset cell "shows `✕`" while the component shows `–` (`:141-145`). All three are fixed in Task 5 as part of the copy that has to change anyway. The `perm-caption` TESTID stays exactly where it is: it is a structural marker in three gates (`scripts/gate-m14-fidelity.mjs:808,992`, `gate-m16-chrome.mjs:274`, `gate-m44-ux-foundation.mjs:774`) and only its text may move.
- **E15 (amends R3/R4) — the token alone does not bind a request to a CHANNEL, and without the second check a stolen one is worth more than the spec assumes.** R3 puts the run token in the request line ("R4's token is what makes a forged line useless") and `runBrokeredOperation` resolves the run from that token alone. But the line is written into `<runDir>/broker.ndjson`, and a run directory is `0700` under the same uid as every other run on the host — so a worker that lists `<state>/slaveofai/runs/` can read a sibling's channel and lift its token, once that sibling has made one request. With the token as the only check, the thief replays it **on its own channel**, and the daemon writes the reply — the brokered operation's bounded OUTPUT — into the thief's own directory, where it can read it. The fix is one comparison and it is not optional: the line carries `runId` as well, the daemon refuses any line whose `runId` is not the directory it was found in (`identity_mismatch`), and a reply is only ever written into the directory the request came from. The residual is then "a stolen token is usable only on the victim's own channel, where its reply lands in the victim's directory", and it is STATED in `apps/orchestrator/src/broker.ts`'s docstring the way `permissions.sh:57-59` used to state its own — beside the two things that reduce it: the directories are named by uuid and outside every worktree, and a served request cannot be replayed because its reply file is its idempotency key.

## 5. Carried backlog (M51's deferred list and M51 §5; not M52 scope unless a task touches the file)
From `.superpowers/sdd/2026-09-12-m51-breaker-cost/final-review-deferred.md`: eight files still write guardrail names as unchecked literals and want `satisfies GuardrailKind`; `MODEL_PRICES` is unpinned against `CLAUDE_CODE_MODELS`, so a seventh model can be silently unpriced; de-escalation lowers a run's WORD while a standing `toolCallCap` keeps it constrained; the candidates' vanished-run path records an escalation about a concluded run (unreachable today); `runTapScript` has no timeout while `preflightTap` runs on every start and resume, so a hanging tap hangs every spawn; a tap that breaks between daemon start and a spawn still fails that spawn; three web read models (`graph.ts`, `overview.ts`, `org.ts`) divide tool calls by `maxToolCallsPerRun` where a capped run's honest denominator is `toolCallCap` — **M52 may fix this where a task touches the file**; `upperBoundUsd`'s flat `RUN_UNMEASURED_CAP_USD` can render below the estimate on the same tile (E20's `max()` ruling); the `brief.test.ts` comment contradicting `unmeasuredRuns`; M4 Cursor's `rejected` line yields no `tool_result`, so a gate-denied Cursor call suppresses every breaker arm until the next result — **M52's default-deny makes Cursor denials commoner and raises this from theoretical to likely**; M7 analytics and the brief compute two differently-scoped upper bounds; M8 `run.tool_result` plus per-turn usage roughly doubles the pump's write volume. From M51 §5 (M50's list and M46–M50 §5): a dispatch from a pre-release snapshot can leave one uncollected worktree; a null-engagement temporary hire downgrades with a warn; `brief.ts`'s team read has no `orderBy`; `gate:m47-team-formation` stage 6 flakes on `recordDecision` committing before `applyDecision`; the CLI's singular worktree wording; a non-temporary hire reusing an unreleased ephemeral worker; a reuse leaving a decision claiming an engagement; `engagement_over` racing a dispatch; `AllSlavesTable` not sorting released rows last; Organization/RunbookPanel/Knowledge each loading a Supervisor world per Overview render; `tierOf` busy at draft time; `CATALOG_ENTRIES_MAX` by id; pre-M47 templates keeping `capabilityKeys: []`; gates m8/m10/m13 outside CI; the m11 flake; m14 PNG nondeterminism; mapper M6/M7/E23; `WorkforceClient`'s bare `<details>`; `q` undebounced; the drawer's spec-scoped `rawOverride`; the `sourceRepository` asymmetry; `GET /api/org/catalog` principal harmonisation; M48's `unknownStages`, persona runbook key collisions, `gate:m48` writing to a seed row, `runbooks show` printing keys, `addRunbook`'s discarded `by`, `stageTitle` vs swapped runbooks; M49's workspace-only verified-FACT command names, company-scope condensation, the unbounded coverage query, summaries never becoming sources, orphan candidates for stranded runs, no end-to-end failing-write test, and the shared `block`/`singleLine`/`safe` helper.
