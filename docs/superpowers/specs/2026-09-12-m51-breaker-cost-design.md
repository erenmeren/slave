# M51 — Behavioural circuit breaker + actual cost

Eighth milestone of the roadmap `2026-09-10-roadmap-m44-m56.md`, extending the guardrails (`evaluateGuardrails`, the sweep's timeout/tool-cap pass), `SlaveRun.costUsd` and the M32 upper-bound policy — named as extensions, per roadmap line 57. Designed 2026-09-11 from an inventory of the six breaches, the ten-member `RuntimeEvent`, the one cost writer, the Claude hook plane, the Supervisor's own limits, the four stop paths, the cost surfaces and the gates that pin them (session scratchpad `m51-explore.md`), with findings — never shapes — borrowed from `munder-difflin`'s breaker. Every **Ruling** took the recommended option under the user's standing approval. "Slave" is this project's word for an AI worker.

**Goal.** A run that is not getting anywhere is stopped in three rungs, each louder than the last: the Supervisor STEERS it with a fixed system sentence, the system CONSTRAINS what is left of its tool budget, the backend STOPS it and lets the existing failure streak count the stop. What the detector reads is persisted, so the verdict is reproducible from the event log months later: every tool call carries its id and a hash of its arguments, every tool result carries `ok`/`error` and nothing else. A quiet twenty-minute build is not a loop — progress is the disjunction of three cheap clocks, debounced. And money stops being one number: Actual is what a provider reported, Estimated is tokens × a fallback-only price table, Upper bound is the safe figure the budget guardrail has always used — three answers to three different questions, only one of which any guardrail believes.

**Facts the design stands on.** `GuardrailBreach.guardrail` is a bare `string` (`packages/domain/src/guardrails/evaluate.ts:29`) and sixteen spellings live as literals across nine production files; `circuit_breaker` (`:115`) means only `consecutiveFailures >= consecutiveFailureLimit`. `appendEvent` validates on write and `packages/events/src/read.ts:23-26` THROWS on a row it cannot parse. `RuntimeEvent` has ten members and no tool result and no usage (`packages/providers/src/types.ts:77-124`); `parseStreamLine` returns exactly one event per line (`claude/stream.ts:27`); Cursor's `completed` line is deliberately folded away (`cursor/stream.ts:294-310`) and `cursor-stream.test.ts:631` is an allow-list of six kinds. `run.tool_call`'s payload is `{ name, summary }` (`events/schema.ts:56-59`) — no id, no args; `summary` collides (munder-difflin #377). `SlaveRun.costUsd` has one writer, at conclusion (`pump.ts:1147`); `Checkpoint.cumulativeCostUsd` is written at pause as `run.costUsd ?? 0` under a SETTLED ruling (M12 Task 9 R4, `pump.ts:297-311`) that nothing reads it for a money decision. There is no pricing table and no estimator anywhere. `SlaveRun` carries `provider` but no `model`; `resolveRuntime` (`packages/control/src/runtime.ts`) is the pair a run is dispatched with. Steering has a primitive: `requestPause` → `queuedMessage` → `claimResume` (`control/pause.ts:14`, `resume.ts:47,215`, `tick.ts:399-428`), and `requestResume` already takes `actor: 'human' | 'system'`. `tierOf` demotes everything to `proposed` under a halt (`policy.ts:47`); `filterFresh` blocks a situation key for `COOLDOWN_MS` = 15 min (`observe.ts:377-390`). `ClaudeSettings` (`claude/settings.ts:9-18`) is the ONE place Claude's hook JSON is spelled, `--include-hook-events` is already on (`flags.ts:39`), and a second producer pushing into `AsyncEventQueue<RuntimeEvent>` (`claude/adapter.ts:335`) is invisible to the pump.

## 1. Rulings

- **R1 — The detector is a pure function over a PERSISTED per-run stream.** §9 Q15 resolves toward persisted: a verdict nobody can re-derive from the log is a verdict nobody can argue with. `run.tool_call`'s payload gains `toolUseId: string` and `argsHash: string` (optional-on-read for pre-M51 rows, required on write — the `run.tool_denied.toolUseId` precedent, `events/schema.ts:60-69`). `argsHash` is `sha256(canonical(input))` hex, computed in `packages/providers/src/hash.ts` `hashToolInput(input)`: keys sorted, every string capped at `HASH_STRING_CAP = 512` code points, arrays at `HASH_ARRAY_MAX = 32` entries, depth at `HASH_DEPTH_MAX = 6`, non-finite numbers and functions dropped — a cap per string, never a slice of the whole serialisation (munder-difflin #377: `inp.slice(0,200)` collided nine Bash commands sharing a path preamble and constrained a working agent). **The args themselves are never persisted** — the event log is not a transcript (`RunContext.prompt` remains the only place a prompt is stored). NEW event `run.tool_result { toolUseId: string, toolName: string, outcome: 'ok' | 'error', errorClass: string | null }` — bounded, no result text; `errorClass` is a normalised token (`api_error`, `timeout`, `not_found`, `permission`, `other`), capped at 40 chars. NEW vendor-neutral `RuntimeEvent` member `tool_result` with the same four fields, produced by Claude's `user`/`tool_result` lines (`claude/stream.ts:47-51`, today `ignored`) AND by Cursor's `completed` line (`cursor/stream.ts:294-310` stops folding it away; `parseCursorLine`'s allow-list at `cursor-stream.test.ts:631` becomes seven kinds — the tripwire fires, and moving it is the proof the member is vendor-neutral). The detector is `packages/domain/src/breaker/detect.ts` `detectBehaviour(window: BreakerWindow): BreakerVerdict` — pure, no clock of its own, `{ level: 'healthy' | 'steer' | 'constrain' | 'stop'; trip: BreakerTrip | null }` where `BreakerTrip = { kind: 'repeated_call' | 'error_storm' | 'no_progress'; detail: string }`. `BreakerWindow` = the run's last `BREAKER_WINDOW = 60` `run.tool_call` / `run.tool_result` / `run.output` rows (id, seq, ts, and for calls the key `toolName:argsHash`), the run's `breakerLevel`/`breakerTrips`/`breakerSteers`, and a `progress: { distinctKey: boolean; worktreeChanged: boolean; output: boolean }` triple the caller measured — three independent cheap clocks, a disjunction and not one signal (munder-difflin `breaker.ts:122-136`). The caller is the sweep: `distinctKey` and `output` come off the window it already loaded; `worktreeChanged` is a THIRD method `worktreeFingerprint(path): Promise<string | null>` on `GitProbe` *(verified: `packages/control/src/git-probe.ts` is `{ isRepository, branchExists }` only — it is the injectable-probe SHAPE the clock borrows, not a probe that can answer "did anything change"; the new method runs `git status --porcelain` + `rev-parse HEAD`, the exact pair `pump.ts:267-270` already takes at pause, hashed, `PROBE_TIMEOUT_MS` and `null` on failure — and a `null` reads as "no evidence either way", which suppresses the arm rather than trips it)*, spawned at most once per run per `BREAKER_BEAT_MS` and never on a tick where no other arm is close. Trips: **repeated_call** — the same key `REPEAT_TRIP_COUNT = 8` times with no intervening distinct key; **error_storm** — `ERROR_STORM_COUNT = 5` consecutive `outcome: 'error'` results; **no_progress** — all three clocks false for `NO_PROGRESS_BEATS = 2` consecutive beats. **A running tool call with no result yet is never a trip**: a trailing `tool_call` whose `toolUseId` has no `tool_result` suppresses every arm — quiet long builds are not loops. Constants in `packages/domain/src/breaker/constants.ts`, domain constants and not workspace settings (the M38 precedent, `supervisor/constants.ts:1-7`), pinned by `packages/domain/test/breaker/constants.test.ts` the way `SUPERVISOR_PER_CALL_CAP_USD` is.
- **R2 — STEER → CONSTRAIN → STOP, one rung per beat; one healthy beat steps back down; action fires only on ESCALATION.** `enum BreakerLevel { none, steered, constrained }` (`stopped` is the run's terminal status, never a level) + `SlaveRun.breakerLevel BreakerLevel @default(none)`, `SlaveRun.breakerTrips Int @default(0)`, and `SlaveRun.breakerSteers Int @default(0)` — *(verified: the direction named two columns; de-escalation lets one run be steered twice, so the `STEERS_PER_RUN_MAX` cap needs a counter that de-escalation does not reset, and `breakerTrips` counts every rung)*. Evaluated by `apps/orchestrator/src/sweep.ts`'s existing pass over live runs, beside `timedOutNow`/`overCapNow` (`:408-409`) and after them (a run past a hard limit is stopped for that reason, not this one), at most once per `BREAKER_BEAT_MS = 60_000` per run so a fast tick loop cannot climb the ladder in three seconds. A verdict one rung above the stored level escalates; `healthy` steps the stored level down one and writes nothing else; equal or lower does nothing at all — a `queuedMessage` re-sent every tick is a second bug (munder-difflin `breaker.ts:44-47`). Each escalation to `steered`/`constrained` appends `run.breaker { level: 'steered' | 'constrained', trip: BreakerTrip['kind'], detail: string }` (54th event, lane `work`, activity card `BreakerCard` tone `warn`, timeline sentence); the STOP rung announces itself as `guardrail.tripped` alone (R3) — one rung, one event, never two names for one fact. De-escalation is silent. A new event touches nine sites and they are listed once, here: `packages/domain/src/events/schema.ts`, `packages/db/prisma/schema.prisma`'s `EventType` + the migration, `packages/db/src/enums.ts`, `packages/domain/src/supervisor/timeline.ts`'s `EVENT_LANE`, `apps/web/src/components/activity/cards.tsx` (component + registry), `apps/web/src/lib/activityFilters.ts`, `apps/web/src/server/timeline.ts`, `packages/domain/test/events/schema.test.ts`, `apps/web/test/activityFilters.test.ts` + `activity-cards.test.tsx`. Both new events pay it.
- **R3 — Three actors, three packages: the Supervisor steers, the system constrains, the backend stops.** **STEER (domain + control).** Situation `run_looping` (16th in `SITUATION_KINDS`, subject = the RUN id — the first kind whose subject is a run; a Postgres enum member and `enum-parity.test.ts` moves with it), facts `{ trip, toolKey|errorClass, repeats, level }`, raised by `observe` from `SupervisorRun`-shaped world rows the loader already reads. One offer `steer_run { runId, slaveId, text }` (16th `ACTION_KIND`), tier **`applied`** — and it is only defensible because **the text is a FIXED system-authored sentence**, never model text: `BREAKER_STEER_TEXT` in `breaker/constants.ts` ("You have made the same tool call N times with no new result. Stop, say in one paragraph what you are stuck on, and either change approach or report why you cannot."), interpolated from the trip's own integers and nothing else. `carryOut` calls `packages/control/src/breaker.ts` `steerRun(runId, trip)`: `requestPause(runId, 'circuit breaker', 'guardrail')`, and on the beat the run reaches `paused`, `requestResume(runId, text, 'circuit breaker', undefined, 'system')` — the round trip that already exists and is already tested (`resume-intent.test.ts`), with the sender tagged `system` so it never lands in the web's "interventions" filter under a person who was never there. At most `STEERS_PER_RUN_MAX = 2` per run; a third trip escalates straight to constrain. The Supervisor is itself limited exactly as it always is: `tierOf`'s halt demotion (`policy.ts:47`) applies unchanged, the per-tick model cap is irrelevant (this situation needs no model call), and `run_looping` is exempt from `COOLDOWN_MS` — `filterFresh` takes a per-kind override and `run_looping` uses `BREAKER_COOLDOWN_MS = 120_000`, because 15 minutes is far too coarse for a loop that burns $5 in three. **CONSTRAIN (control).** `constrainRun(runId)` sets `SlaveRun.toolCallCap = toolCalls + CONSTRAIN_GRACE_CALLS` (30) under the row lock and re-delivers the steer text through the same pause/resume round trip; the sweep's existing ceiling check becomes `run.toolCalls > (run.toolCallCap ?? workspace.maxToolCallsPerRun)` (`sweep.ts:409`) — one comparison, one new column, and the existing `tool_call_ceiling` breach is what then fires. **STOP (orchestrator).** The sweep's own claim/cancel shape, verbatim: `updateMany` to `stopping` (`:424-429`) → `adapter.cancel` (`:455`) → `guardrail.tripped { guardrail: 'behavioural_loop', detail }` (`:460-475`), **no terminal row** — `pump.ts:1039-1098` concludes it `failed`, `verify.ts:295-360` releases the task to `rework` and charges an attempt, and the existing `circuit_breaker` streak counts it. The two breakers compose: enough behavioural stops halt the workspace, which then demotes the Supervisor that was steering. Hard stop is ON (the roadmap says the backend stops); **`Workspace.breakerStopEnabled` is NOT added** — a ladder whose top rung is configurable off is a ladder that ends at `constrained`, and per-workspace thresholds are out of scope by the same M38 §8 rule the constants follow.
- **R4 — Labels never keys, for guardrails too.** `packages/domain/src/guardrails/kinds.ts`: `GUARDRAIL_KINDS` — the closed list of the sixteen spellings already written plus `behavioural_loop` (`emergency_stop`, `concurrency`, `global_concurrency`, `budget_exhausted`, `budget_warning`, `circuit_breaker`, `behavioural_loop`, `run_timeout`, `tool_call_ceiling`, `pause_gate`, `permission_mode`, `no_planner`, `no_reviewer`, `review_retry_cap_exhausted`, `merge_failure`, `verify_could_not_run`, `verify_failed`) — plus `GuardrailKind` and `GUARDRAIL_LABEL: Record<GuardrailKind, string>` (the `SITUATION_KINDS` pattern). `GuardrailBreach.guardrail` becomes `GuardrailKind`, and each of the nine production files that spells one (`sweep.ts`, `pump.ts`, `planning.ts`, `review.ts`, `merge.ts`, `tick.ts`, `emergency.ts`, `evaluate.ts`, `gate-m38-supervisor.mjs`) imports the constant. **The `guardrail.tripped` payload stays `z.string()`** *(verified: `packages/events/src/read.ts:23-26` throws on a row the domain cannot parse, so a `z.enum` would make the whole forward read of any database holding a spelling this list forgot unreadable — a closed union types the PRODUCERS, where a mistake is a build error, and the log stays forgiving)*. `GuardrailTrippedCard` (`cards.tsx:375-390`) prints `GUARDRAIL_LABEL[kind] ?? kind` and keeps the raw key in `title`/`data-guardrail` (`docs/ia.md` rule 3); `apps/web/test/integration/activity-history.test.ts:154`'s `guardrail: 'budget'` fixture moves to `budget_exhausted`.
- **R5 — Three cost figures; the guardrail's number does not move.** **Actual** = provider-reported: `SlaveRun.costUsd`, unchanged, terminal-only writer, `null` never `0`. **Estimated** = tokens × a fallback-only table: new `packages/providers/src/pricing.ts` — `MODEL_PRICES: Record<string, { inputPerMTok: number; outputPerMTok: number }>` keyed by model id with the `CLAUDE_CODE_MODELS` aliases (`default`/`opus`/`sonnet`/`fable`/`haiku`) resolved through `PRICE_ALIASES`, `source: 'static'`, and `estimateCostUsd(modelId, tokens): number | null` returning `null` for an unpriced id — **the estimate is fallback-only and may never overwrite a reported figure** (munder-difflin's cost bug #1 was one Sonnet price for every model; Opus undercosted ~5×). New `SlaveRun.model String?` written at `apps/orchestrator/src/runs.ts:44` from the `resolveRuntime` pair that already writes `provider` — the same M12 Task 6 reasoning, that the profile chain can change under a live run. Per-run provenance is DERIVED, not stored: `costProvenanceOf(run): 'reported' | 'estimated' | 'unmeasured'` in `packages/domain/src/guardrails/spend.ts` (`costUsd !== null` → reported; else priced tokens → estimated; else unmeasured). NEW vendor-neutral `RuntimeEvent` member `usage { input: number; output: number }`, produced from Claude's per-turn `assistant` lines and consumed by the pump into `SlaveRun.tokensIn/tokensOut` MID-RUN, so a live runaway is visible before it concludes; Cursor produces none (`reportsCost: false`, no tokens at all) and a new capability flag records it. *(verified: `parseStreamLine` returns exactly ONE `RuntimeEvent` per line and a turn's `usage` rides the same `assistant` line as its `tool_use`, so the member cannot come from `parseAssistantLine` without either dropping usage on every tool-calling turn or changing one parser's contract into an array; instead a second pure function `parseStreamUsage(raw): { input, output } | null` in `claude/stream.ts` is called by the adapter beside `parseStreamLine` and pushes the `usage` event into the same `AsyncEventQueue` — the exact seam R6's tap uses, and `parseStreamLine` keeps its one-line-one-event contract and its exhaustiveness tests.)* The billed-input asymmetry is named out loud: `RunOutcome.tokens.input` folds cache creation and cache read in deliberately, **for cost** (`types.ts:53-63`), while a token BUDGET should exclude cache read (it re-bills a fixed context every request and turns a cap into a session-length timer, measured at munder-difflin `usage.ts`); M51 estimates cost, so it uses the folded figure and says so in `pricing.ts`'s docstring. `Checkpoint.cumulativeCostUsd` / `cumulativeTokens` get their real writer *(verified: they are not dead — `pump.ts:311,327` writes `run.costUsd ?? 0` at pause, always `0` in practice, under the SETTLED M12 Task 9 R4 ruling that the column stays NOT NULL because nothing reads it for a money decision. That ruling is kept intact: the pause write becomes `estimateCostUsd(run.model, {tokensIn, tokensOut}) ?? run.costUsd ?? 0` and `cumulativeTokens = tokensIn + tokensOut`, which mid-run are now real (the `usage` event), and the only reader is a DISPLAY line. No guardrail, no sum, no comparison — the settled sentence still holds.)* **Upper bound** = the existing `workspaceSpend()` total **plus** concluded unmeasured runs × `RUN_UNMEASURED_CAP_USD = 1` (beside `SUPERVISOR_PER_CALL_CAP_USD` in `packages/domain/src/guardrails/spend.ts`, pinned by `spend.test.ts`) — **a DISPLAY figure only, computed in `apps/web/src/server/brief.ts`, never inside `workspaceSpend`**. `stats.spentUsd`, `evaluateGuardrails` and the budget breach are UNCHANGED. This is an explicit ruling and the asymmetry is the reason: an unmeasured Supervisor CALL is charged at its cap because it is finished and nothing will ever report it; an unmeasured RUN is counted and charged nothing, because charging it would let a budget halt fire on spending nobody measured — which `evaluate.ts:80-95` and `stats.ts:183-187` both refused in writing, and which would move `gate:m38` stage 3 (`gate-m38-supervisor.mjs:545-588`, which asserts `facts.reason === 'budget_exhausted'` precisely so another halt cannot pass that stage). Showing the bound and charging for it are different acts; M51 does the first only. One shared real-money formatter `apps/web/src/lib/realMoney.ts` (`formatUsd(n: number | null): string`, `null` → `—`), beside `money.ts`'s simulated-money note, replacing the inline `.toFixed(2)` on every real-money surface.
- **R6 — The hook spike, bounded to one measurement.** Claude only. `ClaudeSettings` (`claude/settings.ts:9-18`, the ONE place Claude's hook JSON is spelled) gains a `PostToolUse` registration with matcher `"*"` pointing at `scripts/tool-result-tap.sh`, a sibling of `scripts/pause-gate.sh`, carried across process boundaries the way `hookPath` is (`ClaudeCodeAdapterOptions.tapPath`, `Checkpoint.hookPath`'s pattern) and pre-flighted like the pause gate (`runtime/gate-preflight.ts`, `expectAllow: { kind: 'silent' }`). The tap writes ONE bounded NDJSON line per call — `{ toolUseId, toolName, outcome, errorClass }`, `TAP_LINE_MAX_BYTES = 4096`, never the tool input or the result text — into `join(runDir, 'tool-results.ndjson')`; the adapter tails it and pushes `tool_result` events into the SAME `AsyncEventQueue<RuntimeEvent>` (`claude/adapter.ts:335`) as a second producer, invisible to `pump.ts`. **The stream wins**: dedupe by `toolUseId` in the adapter, first arrival kept, so the tap only fills a gap. Capability `reportsToolResults: boolean` joins `ProviderCapabilities` (`claude_code` true, `cursor` true — its `completed` line is a real tool result; the table may only ever be WIDENED by proof, `capabilities.ts:76-81`). No socket, no HTTP sink, no reading `~/.claude/projects/**` — transcript location is an undocumented, versioned convention that fails SILENTLY (an absent directory reads as "no transcripts yet"), and nothing in M51 may depend on it. The spike's deliverable is the MEASUREMENT, recorded in §4 errata: which of stream and tap arrives first, how often each is absent, and whether the tap earns its second pre-flight — and if it does not, the honest outcome is a one-line erratum saying so and the tap staying unregistered.
- **R7 — Surfaces.** Brief cost tile (`apps/web/src/components/project/ProjectBrief.tsx:194-213`): three LABELLED lines — `actual $X` (reported), `estimated $Y` (priced tokens, reported runs included as their reported figure), `upper bound $Z` (the display total of R5) — plus the two existing hole sentences. This is the "three different questions" resolution of the one-figure rule (`brief.ts:78-82`): exactly one of the three, `spentUsd`, is still the guardrail's number, and the tile says which. *(verified: `gate:m45`'s eight-facts pin is `EXPECTED_BRIEF_FACTS` at `scripts/gate-m45-project-experience.mjs:95-105`, a list of tile `fact` names in render order — it does NOT move, because the cost tile is still one of eight. What moves is stage 1's cost TEXT expectations at `:766-767`.)* `ProjectBrief.cost` gains `actualUsd`, `estimatedUsd`, `upperBoundUsd`, keeping `spentUsd`/`measuredUsd`/`unmeasuredCalls`/`unmeasuredRuns` untouched. Task drawer (`TaskDetailPanel.tsx:81-82,560-572`): the Cost group sums reported and estimated per run, names each run's provenance, and prints `retried work $W` — the cost of every implementation run of the task but its last, beside `Task.attempt` *(verified: `SlaveRun` carries no attempt column and M51 does not add one; "which attempt" is the run's ordinal within the task's implementation runs, which the drawer already lists in order)*. Run card word: `UserCardState` gains `steered` and `constrained` with `USER_CARD_LABEL` entries `STEERED` / `CONSTRAINED`, and `userRunStatus(status, facts?: UserCardFacts)` takes an OPTIONAL second argument `{ breakerLevel?: BreakerLevel }` — the `UserTaskFacts` precedent for "facts a status alone cannot say" (`status/user.ts:88-110`), optional so `tones.ts:51-53`'s ~20 one-argument call sites keep reading as they always did; a `working` run at level `constrained` reads `CONSTRAINED`. Analytics (`server/analytics.ts:223-224`): the existing `note` convention gains `upper bound $Z with N runs unmeasured`. Activity: the `BreakerCard` of R2 and the relabelled guardrail card of R4. Office `FocusCard` is unchanged (out of scope). `gate:m44` RAW_TOKENS gains `GUARDRAIL_KINDS` and the two `BreakerLevel` bare words.
- **R8 — Gate.** `gate:m51-breaker` (real daemon + fake CLI + Playwright), gate #26. Two new fake fixtures: `loop.ndjson` (the same `tool_use` with byte-identical `input` repeated twelve times, each followed by its `user`/`tool_result` line, then a terminal `result`) and `error-storm.ndjson` (six tool calls whose results are `is_error: true`). Stages: (1) the loop fixture runs → `run.tool_call` rows carry `toolUseId` and an `argsHash` that is EQUAL across the twelve and DIFFERENT from a thirteenth call with one changed argument (the #377 negative, asserted); (2) STEER — `run.breaker { level: 'steered', trip: 'repeated_call' }`, a `steer_run` decision `applied`, the run paused and resumed with `queuedMessage` equal to `BREAKER_STEER_TEXT`, and the `run.resume_requested` row's actor is `system`; (3) CONSTRAIN — the next beat sets `SlaveRun.toolCallCap = toolCalls + 30` and `breakerLevel = constrained`; (4) STOP — `guardrail.tripped { guardrail: 'behavioural_loop' }`, run `failed`, task `rework`, `Task.attempt` incremented; (5) the error-storm fixture trips `error_storm` on its own; (6) the NEGATIVE that matters — a fixture with one long-running tool call and no result for three beats trips NOTHING (quiet long builds are not loops); (7) three cost figures on the brief with a measured run (`costUsd: 3.5`), an unmeasured run (`costUsd: null`, no tokens) and an estimated run (`costUsd: null`, tokens present, a priced `model`), and `stats.spentUsd` is byte-equal to what it was before M51 for the same rows; (8) the mid-run token update — `SlaveRun.tokensIn` is non-null while the run is still `working`; (9) the guardrail LABEL (not the key) on the activity card, with the key on `data-guardrail`. `gate:m38` stage 3 unchanged, and asserted unchanged. README roster 25 → 26 (`README.md:828-835`); CI step after `gate:m50-ephemeral` (`.github/workflows/*.yml:78`). The cost tile and the run card both change what a person sees, so the `overview` `gate:m14-fidelity` PNG is regenerated once, in its own deliberate commit (the M50 precedent); no route moves, so the other five are untouched.
- **Never a real model call (the steer text is a constant, not a draft); one vitest at a time; `web:build` last, never beside `next dev`; vocabulary `slave`; migrations additive on both databases with the Prisma 7 diff proof; `decide()` unchanged; `workspaceSpend`/`stats.spentUsd`/`evaluateGuardrails`'s budget arm unchanged and `gate:m38` stage 3 unchanged; the M45–M50 gates unchanged; a refusal after a write inside a transaction throws; refusal kinds in three homes; labels never keys.**

## 2. Surfaces after M51
Domain: `breaker/{detect,constants}.ts`, `guardrails/kinds.ts` (`GUARDRAIL_KINDS`, `GUARDRAIL_LABEL`, `GuardrailKind`), `costProvenanceOf` + `RUN_UNMEASURED_CAP_USD` in `guardrails/spend.ts`, situation `run_looping`, action `steer_run`, events `run.tool_result` / `run.breaker`, `run.tool_call` payload +2 fields, `UserCardState` +2 members and `UserCardFacts`, `filterFresh`'s per-kind cooldown. Providers: `RuntimeEvent` members `tool_result` + `usage`, `hash.ts`, `pricing.ts`, `parseStreamUsage`, Cursor's `completed` arm, `ClaudeSettings.PostToolUse`, the tap tail + dedupe, `ProviderCapabilities.reportsToolResults`. DB: `BreakerLevel` enum, `SlaveRun.{breakerLevel,breakerTrips,breakerSteers,toolCallCap,model}`, two `EventType` members, one `SituationKind` member, the additive migration. Control: `breaker.ts` (`steerRun`, `constrainRun`), `requestPause`'s actor, the `carryOut` arm, the world's `SupervisorRun` breaker fields. Orchestrator: the sweep's breaker pass and per-run cap, the pump's `tool_result`/`usage` arms and the checkpoint's real cumulative write, `runs.ts`'s `model`. Web: brief cost tile + `ProjectBrief.cost` +3, task drawer Cost group, `realMoney.ts`, run card word, Analytics note, two activity cards. Gate m51; `scripts/tool-result-tap.sh`; fixtures `loop.ndjson`, `error-storm.ndjson`.

## 3. Out of scope
Per-workspace breaker thresholds or a breaker settings UI (M38 §8's rule, unchanged); `Workspace.breakerStopEnabled`; Cursor hooks (`PostToolUse` has no measured Cursor twin); reading `~/.claude` transcripts; changing the budget guardrail's number, `workspaceSpend`, or what `stats.spentUsd` charges for; token-velocity and floor-wide cost trips, and the compaction exemption they would need; a `SlaveRun.attempt` column; permissions and the capability broker (M52); evidence-based ranking (M53); the Office FocusCard.

## 4. Errata — where execution corrects this spec

Found while writing the plan (2026-09-11, before Task 1); each is baked into `docs/superpowers/plans/2026-09-12-m51-breaker-cost.md`. Reasoning with file citations is in the plan's companion notes (session scratchpad `m51-plan-notes.md`); summarised here.

- **E1 (amends R5) — `pricing.ts` cannot live in `packages/providers`.** `apps/web/package.json` lists `@slave-of-ai/control`, `@slave-of-ai/db`, `@slave-of-ai/domain`, `@slave-of-ai/events` and `@slave-of-ai/simulation` — **not** `@slave-of-ai/providers**, and `apps/web/src/lib/providerLabel.ts:13-41` records why that barrel must not be reached from a client component (it re-exports `claude/adapter.ts`, which imports `node:child_process`). The brief, the drawer and Analytics all need the estimate. The table and `estimateCostUsd` therefore live in **`packages/domain/src/guardrails/pricing.ts`** — pure data and one pure function, importable by web, control, orchestrator and providers alike. `packages/providers/src/models.ts` keeps the id/label listing it already owns.
- **E2 (amends R1) — the vendor-neutral `tool_call` must carry `argsHash` too.** The pump only ever sees a `RuntimeEvent` (`apps/orchestrator/src/pump.ts:642-663`); `run.tool_call`'s payload cannot gain a field the event it is built from does not have. `RuntimeEvent`'s `tool_call` member gains `argsHash: string`, computed by BOTH parsers (`packages/providers/src/claude/stream.ts:411-416` from the `tool_use` block's `input`, `packages/providers/src/cursor/stream.ts:357-363` from the tool object's `args`), and `hashToolInput` is a providers-side pure function because that is where both parsers are.
- **E3 (amends R1) — Cursor's `completed` line carries no `is_error`.** The measured shape (`packages/providers/test/fixtures/cursor/cursor-run.ndjson` line 7) is `{"readToolCall":{"args":{...},"result":{"success":{...}}}}`, and the one other measured result shape is `result.rejected` (`fixtures/cursor/gate/run-2-flag-present.ndjson`). So `outcome` is `'ok'` when `result` has a `success` key and `'error'` otherwise; `rejected` is checked FIRST and still returns `permission_denied` (`cursor/stream.ts:320-337`), which keeps one line to one event.
- **E4 (amends R5) — assistant-line usage does not sum to the `result` line's figure, and the terminal write must stop erasing it.** Measured on `packages/providers/test/fixtures/complete.ndjson`: the four assistant lines report `output_tokens` 2 + 2 + 20 + 3 = **27** while the `result` line reports **741**; input is 8 against 4. The mid-run figure is therefore an explicit **floor**, said so in the column's docstring and pinned by a test that asserts the two disagree. And `writeStreamUsage` (`apps/orchestrator/src/pump.ts:220-233`) currently writes `tokensIn: outcome.tokens?.input ?? null` — with a mid-run writer in place that ERASES a real measurement on every degraded `result` line. It returns early instead when `outcome.tokens === null`.
- **E5 (amends R2) — the beat and the debounce need two columns the spec did not name.** `SlaveRun` has no `updatedAt` (`packages/db/prisma/schema.prisma:696-766`), so "at most once per `BREAKER_BEAT_MS` per run" has no clock to read; and `no_progress` is "all three clocks false for `NO_PROGRESS_BEATS = 2` **consecutive beats**", which a pure detector cannot remember. The migration adds `breakerBeatAt DateTime?` and `breakerQuietBeats Int @default(0)` beside the five R2 names.
- **E6 (amends R2) — the sweep `continue`s before the breaker could ever run.** `apps/orchestrator/src/sweep.ts:410` is `if (!timedOutNow && !overCapNow) continue`, so a healthy run leaves the loop body before line 412. The breaker pass goes INSIDE that branch (`{ await beatBreaker(...); continue }`), which is also exactly what R2's "after them" means: a run past a hard limit is stopped for that reason and never reaches the breaker.
- **E7 (amends R1) — `GitProbe` is a two-method interface with a live injection seam.** `packages/control/src/git-probe.ts:10-13` is `{ isRepository, branchExists }`, injected by `useGitProbe` (`packages/control/src/workspace.ts:132`); a third REQUIRED method breaks every fake. A separate `WorktreeProbe { fingerprint(path): Promise<string | null> }` and `realWorktreeProbe` land beside it in the same file, injected on `SweepDeps` — the shape R1 wanted, with none of the breakage.
- **E8 (amends R3) — a steer is two phases, because `requestPause` does not produce a `paused` run.** `requestPause` claims `pause_requested` (`packages/control/src/pause.ts:53-58`); only the pump reaches `paused`, after the gate denies a call. `requestResume` refuses anything but `paused` (`packages/control/src/resume.ts:126`). And `paused` is not in `SWEEPABLE` (`apps/orchestrator/src/sweep.ts:41,50`), nor would it survive `run.pid === null` at `:398`. So `steerRun` does phase A only — claim the pause and queue the text in one call — and a NEW per-tick pass, `deliverBreakerSteers`, does phase B on whichever tick finds the run `paused`. Its predicate is derivable and needs no column: `breakerLevel != none` AND `status = paused` AND `pauseReason = guardrail` AND `queuedMessage != null` AND `resumeRequestedAt = null`. Nothing else in the tree pauses with a queued message (`tick.ts:253` is the only other `'guardrail'` pause and it queues nothing).
- **E9 (amends R3) — `SupervisorWorld` has no runs at all.** There is no `SupervisorRun` type (`packages/domain/src/supervisor/world.ts:271-316` lists eleven collections and none of them is runs); `SupervisorSlave.busy` is `row.runs.length > 0` (`packages/control/src/supervisorWorld.ts:764`) and is the only run-derived fact in the world. `run_looping` cannot be raised from rows the loader "already reads". `SupervisorWorld` gains `runs: readonly SupervisorRun[]`, and the loader gains one `slaveRun.findMany` over non-terminal runs plus — only when some loaded run is at a level above `none` — one `DISTINCT ON (e."runId")` read of the newest `run.breaker` per run, modelled line for line on `loadLatestGuardrails` (`supervisorWorld.ts:271-285`).
- **E10 (amends R5) — `SlaveRun.model` is not written at `runs.ts:44`.** `createRunUnlessArchived` (`apps/orchestrator/src/runs.ts:31-47`) is a generic archive-guarded insert that names no provider. `provider` is written STRICTLY AFTER `adapter.start()` returns, at three `slaveRun.update` sites — `apps/orchestrator/src/tick.ts:696-712`, `planning.ts:574`, `review.ts:520` — which is the rule `packages/domain/src/guardrails/spend.ts:54-64` is built on. `model: resolved.model ?? null` joins those three, and `resume.ts:117` carries `checkpoint.model` the way it already carries `checkpoint.provider`.
- **E11 (amends R6) — `preflightGate` cannot pre-flight a tap.** It arms and disarms a pause flag and asserts BOTH directions (`packages/providers/src/runtime/gate-preflight.ts:98-141`); a PostToolUse tap never denies, so the armed direction fails by construction. The tap gets `preflightTap({ tapPath })` in the same file: one spawn, a synthetic PostToolUse payload on stdin, `SLAVEOFAI_TOOL_RESULTS` pointed at a temporary file, and three assertions — exit 0, EMPTY stdout (a tap that speaks would be read by the CLI as a hook response), and exactly one line written that parses to the four fields.
- **E12 (amends R2) — 53 event types today, so the two new ones are the 54th and 55th.** `packages/domain/src/events/schema.ts` has 53 `z.literal` arms and `packages/domain/test/supervisor/timeline.test.ts:18-19` asserts `LANE_BY_TYPE` has 53 keys. `run.tool_result` is the 54th (lane `null`, like `run.tool_call` beside it) and `run.breaker` the 55th (lane `work`); the count moves 53 → 55.
- **E13 (amends R7) — `actual` REPLACES the `measured $X` line, it does not join it.** `ProjectBrief.cost.measuredUsd` is `spend.runsMeasuredUsd + spend.supervisorMeasuredUsd` (`apps/web/src/server/brief.ts:295`) — which is exactly R5's Actual, to the cent. Printing both would be two labels on one number. The tile therefore grows by TWO lines, not three, which matters because `gate:m45` stage 1 fails any tile whose bottom edge falls below the 900px fold (`scripts/gate-m45-project-experience.mjs:750-757`). Its stage-1 cost expectations at `:766-767` (`'$25'`, `'unmeasured calls charged at $1.00 each'`) do NOT move: both lines survive verbatim.
- **E14 (amends R5/R7) — `SpendRow` cannot answer "estimated".** It is `{ costUsd, provider, status }` (`packages/domain/src/guardrails/spend.ts:24-33`) and the shared read selects exactly those three columns (`apps/web/src/server/overview.ts:481-484`). The domain gains `CostRow` (a `SpendRow` plus `tokensIn`, `tokensOut`, `model`), the select grows three columns, and `ProjectBriefReads.spendRows` is typed `readonly CostRow[]`. `sumSpend`/`sumSpendFromGroups` and `packages/domain/test/spend-groups.test.ts` are untouched — a `CostRow` IS a `SpendRow`.
- **E15 (amends R4) — adding `GUARDRAIL_KINDS` to `gate:m44`'s blocklist makes the existing guardrail card a leak.** Sixteen of the seventeen spellings contain `_`, so the derived filter (`scripts/gate-m44-ux-foundation.mjs:141-175`) really does take them, and `GuardrailTrippedCard` prints `label={payload.guardrail}` raw today (`apps/web/src/components/activity/cards.tsx:385`). The label fix is not cosmetic and it is not optional; Task 5 ships it and Task 6 adds the tokens, in that order, with a grep step that proves no OTHER surface prints a guardrail key.
- **E16 (amends R8) — there is no per-run WORK fixture knob, so one daemon cannot plan normally and loop on work.** `m8-flow`'s work arm is the `m8a-flow` body verbatim (`packages/providers/test/fake-claude.mjs:31-44`) — it writes a file and COMMITS it, which would move the worktree clock and suppress every trip. `fake-claude.mjs` gains `--work-fixture <name>`, the exact shape of `--plan-fixture` (`:243-258`) and `--review-fixture` (`:260-273`), and that arm REPLAYS ONLY — no write, no commit — so the quiet-worktree clock says what it means.
- **E17 (amends R1) — `errorClass` is a closed PRODUCER union and a forgiving string on the wire.** R4's own reasoning (`packages/events/src/read.ts:23-26` throws on a row the domain cannot parse) applies to the new payload too: `z.string().max(40).nullable()` in the schema, `ToolErrorClass` (`'api_error' | 'timeout' | 'not_found' | 'permission' | 'other'`) in `packages/providers/src/tool-result.ts` where a mistake is a build error.
- **E18 (amends R8) — both new fixtures must end with the routine `Stop` hook line.** `packages/providers/test/fake-claude.test.ts:76-96` enumerates every `*.ndjson` directly under `test/fixtures/` and asserts its last parseable line is `{ type: 'system', subtype: 'hook_response', hook_event: 'Stop' }`. `loop.ndjson` and `error-storm.ndjson` are in that namespace (that is how `--fixture` finds them) and both carry one.

## 5. Carried backlog (M50's deferred list and M50 §5; not M51 scope unless a task touches the file)
From `.superpowers/sdd/2026-09-12-m50-ephemeral/final-review-deferred.md`: a dispatch decided from a pre-release snapshot can insert one more `SlaveRun` after `releaseWorker` commits, leaving one uncollected worktree until the sweep ages it; a `temporary` hire action with a null engagement is downgraded to a project hire with a warn (unreachable today); `brief.ts`'s team read has no `orderBy` and the tile slices five, so a released member can displace a working one; `gate:m47-team-formation` stage 6 is flaky because `recordDecision` commits `applied` before `applyDecision` carries the action out (`packages/control/src/supervisor.ts:162` vs `apps/orchestrator/src/supervisor.ts:308-310`); the CLI's singular `1 worktree collected` wording is asserted nowhere — cover `plural()`; M2 a non-temporary hire can reuse an unreleased EPHEMERAL worker, so a project-wide gap gets coverage that vanishes at `engagement_over`; M3 a `temporary: true` decision carried out as a reuse leaves the decision row claiming an engagement the worker never took; M4 `engagement_over` racing a dispatch ends in a `failed` decision plus the 15-minute cooldown before the release is re-offered; M5 `AllSlavesTable` greys released rows but does not sort them last and keeps the full action set. From M50 §5: Organization/RunbookPanel/Knowledge each load a Supervisor world per Overview render; `tierOf` busy at draft time; `CATALOG_ENTRIES_MAX` by id; pre-M47 templates keep `capabilityKeys: []`; gates m8/m10/m13 outside CI; m11 flake; m14 PNG nondeterminism; mapper M6/M7/E23; WorkforceClient bare `<details>`; `q` undebounced; drawer `rawOverride` spec-scoped; `sourceRepository` asymmetry; `GET /api/org/catalog` principal harmonisation; M48: `unknownStages` not on the panel, persona runbook keys collide by name, `gate:m48` writes to a seed row, `runbooks show` prints keys, `addRunbook` `by` discarded, `stageTitle` vs swapped runbooks; M49: verified FACT names workspace commands only, company-scope condensation, unbounded coverage query, summaries never become sources, orphan candidates for stranded runs, no end-to-end failing-write test, shared `block/singleLine/safe` helper.
