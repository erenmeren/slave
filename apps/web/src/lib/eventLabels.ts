/** Moved to `@slave-of-ai/domain` (`packages/domain/src/feed/eventLabels.ts`) -- see
 *  `./happening.ts` for why. Re-exported under the names this path has always exported. */
export { EVENT_PREFIX_LABEL, eventPrefixLabel, readableEventType } from '@slave-of-ai/domain'
